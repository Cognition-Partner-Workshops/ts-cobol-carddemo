# 01_target_state — pointer

Authoritative artifact: `functional/CARDDEMO/CardDemo_target_state.md` (same repo/branch).

Summary: NO target stack decided yet. All profiles (CORE, ONLINE, BATCH, SUBTRANSACTION,
DATA/BOUNDARY) are PROPOSED with options (Java 21/Spring Boot vs C#/.NET 8; Angular vs React vs
API-only; PostgreSQL vs Aurora vs SQL Server; Spring Batch restart model; dataset mapping policy).
Blocking at STOP A until the user confirms per surface. No downstream playbook may run against
this document until it is marked CONFIRMED.
