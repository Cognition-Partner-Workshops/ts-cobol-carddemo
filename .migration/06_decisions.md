# 06_decisions — dated decision log (append-only)

Schema: | Date | Decision | Options considered | Decided by | Ref |

| Date | Decision | Options considered | Decided by | Ref |
|---|---|---|---|---|
| 2026-09-15 | New Java/Spring Boot engagement started. Owner instruction: Java 21 + Spring Boot + PostgreSQL, single repo, Spring Batch, simple web UI replacing 3270 screens, in-repo equivalents for VSAM/DB2/IMS/MQ/Assembler boundaries, checklist verification + evidence at sign-off, one child per wave, STOP D notify-only, STOP A/B/C/E blocking with pre-filled recommendations (batch A+B). This supersedes the prior engagement's confirmed .NET 8 + Angular target for THIS run. | continue prior .NET engagement vs new Java engagement per explicit owner instruction | project owner (session instruction) | .migration/00_context.md |
| 2026-09-15 | Adopted the existing Java port on `devin/1787158883-cobol-to-spring-boot` (open PR #83, verified `mvn clean verify` green) as the engagement code baseline; engagement branch `devin/1789516557-carddemo-java-engagement` forked from it. Every stream still runs the full artifact + parity + sign-off loop; baseline code is treated as pre-existing implementation to verify, not certified work. PR #83 becomes moot once the engagement branch merges; left open for the owner. | adopt PR #83 baseline vs greenfield rebuild vs adopt app-java branch | Devin (logged; sensible default under owner "pick the sensible option") | spring-boot/; .migration/05_progress.md |
| 2026-09-15 | Reused prior-engagement source-side artifacts (inventory, stream analyses, boundary register, conventions, glossary, program FR business rules) into `functional/CARDDEMO/` and `.migration/`, with provenance noted; target-side content (plans, target_state) rewritten for Java. | re-derive all analysis from scratch vs reuse source-side analysis | Devin | functional/CARDDEMO/CardDemo_inventory.md |
| 2026-09-15 | UI target = server-rendered Thymeleaf pages inside the Spring Boot app ("simple web UI"), single Maven toolchain, no separate SPA build. | Thymeleaf vs React/Vue SPA vs API-only | Devin (PROPOSED for STOP A) | functional/CARDDEMO/CardDemo_target_state.md |
| 2026-09-15 | Write-target allowlist for the dbx guard: `.migration/allowed_targets.json` catalogs=["carddemo"] — the local/dev Postgres database is the only sanctioned write target; legacy source (`app/`) is read-only. | — | Devin | .migration/allowed_targets.json |
| 2026-09-15 | Module-level boundaries re-decided for the Java engagement: B-001 async job launch via REST+BatchJobLauncherService; B-002 wait step -> Java tasklet; B-003 COBDATFT -> Java utility; B-004 CEE3ABD -> exit-code contract; B-005 IMS -> Postgres tables+JPA; B-006 DB2 -> JPA/Postgres; B-007 MQ -> in-process queue seam (no broker, demo); B-008 scheduler conditions -> documented job-order config + exit codes; B-009 VSAM -> Postgres; B-010 datasets -> flat files; B-011 COCRDSEC absent -> DEFERRED (S-13 descoped); B-012 return routing -> web nav seam. | per owner "in-repo equivalents" instruction | Devin | .migration/04_boundary_register.md |
