import { Component } from '@angular/core';

/** Placeholder landing route for admin users; the menu itself is wave 3 (COADM01C). */
@Component({
  selector: 'app-admin-menu',
  standalone: true,
  template: '<p class="placeholder">Admin Menu (coming in wave 3)</p>'
})
export class AdminMenuComponent {}
