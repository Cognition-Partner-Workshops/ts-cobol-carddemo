import { Component } from '@angular/core';

/** Placeholder landing route for regular users; the menu itself is wave 3 (COMEN01C). */
@Component({
  selector: 'app-main-menu',
  standalone: true,
  template: '<p class="placeholder">Main Menu (coming in wave 3)</p>'
})
export class MainMenuComponent {}
