{
  "name": "@carddemo/batch",
  "version": "0.1.0",
  "private": true,
  "description": "CardDemo batch jobs (posting, interest, statements, reports, archival, exports) — modernizes the legacy JCL/Control-M suite.",
  "type": "commonjs",
  "bin": {
    "carddemo-batch": "dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "@carddemo/shared": "*",
    "@prisma/client": "^5.22.0",
    "commander": "^12.1.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "@types/node-cron": "^3.0.11",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
