{
  "name": "@carddemo/shared",
  "version": "0.1.0",
  "private": true,
  "description": "CardDemo shared domain types, enums, and zod validation helpers. Owns openapi.yaml, the REST API contract.",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.17.6",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
